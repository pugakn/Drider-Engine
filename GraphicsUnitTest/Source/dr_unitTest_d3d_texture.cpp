#pragma once
#include <dr_texture.h>
#include <gtest\gtest.h>
#include <dr_d3d_device.h>
#include <dr_d3d_device_context.h>
#include <d3d11.h>
#include <dxgi.h>

TEST(D3DTexture, createFromMemory) {
  EXPECT_TRUE(true);
}
TEST(D3DTexture, createEmpty) {
  EXPECT_TRUE(true);
}
TEST(D3DTexture, map) {
  EXPECT_TRUE(true);
}
TEST(D3DTexture, set) {
  EXPECT_TRUE(true);
}
TEST(D3DTexture, release) {
  EXPECT_TRUE(true);
}
TEST(D3DTexture, unmap) {
  EXPECT_TRUE(true);
}
TEST(D3DTexture, udpateFromMemory) {
  EXPECT_TRUE(true);
}
TEST(D3DTexture, generateMipMaps) {
  EXPECT_TRUE(true);
}