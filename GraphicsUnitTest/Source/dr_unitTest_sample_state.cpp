#pragma once
#include <dr_graphics_defines.h>
#include <gtest\gtest.h>

TEST(SamplerState, destructor) {
  EXPECT_TRUE(true);
}

TEST(SamplerState, create) {
  EXPECT_TRUE(true);
}

TEST(SamplerState, set) {
  EXPECT_TRUE(true);
}

TEST(SamplerState, release) {
  EXPECT_TRUE(true);
}