#pragma once
#include <dr_graphics_defines.h>
#include <gtest\gtest.h>

TEST(InputLayout, destructor) {
  EXPECT_TRUE(true);
}

TEST(InputLayout, create) {
  EXPECT_TRUE(true);
}

TEST(InputLayout, set) {
  EXPECT_TRUE(true);
}

TEST(InputLayout, release) {
  EXPECT_TRUE(true);
}