#pragma once
#include <dr_graphics_defines.h>
#include <gtest\gtest.h>

TEST(Shader, destructor) {
  EXPECT_TRUE(true);
}

TEST(Shader, set) {
  EXPECT_TRUE(true);
}

TEST(Shader, release) {
  EXPECT_TRUE(true);
}

TEST(Shader, createFromMemory) {
  EXPECT_TRUE(true);
}