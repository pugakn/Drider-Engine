#pragma once
#include <dr_graphics_defines.h>
#include <gtest\gtest.h>

TEST(RasterizerState, destructor) {
  EXPECT_TRUE(true);
}

TEST(RasterizerState, create) {
  EXPECT_TRUE(true);
}

TEST(RasterizerState, set) {
  EXPECT_TRUE(true);
}

TEST(RasterizerState, release) {
  EXPECT_TRUE(true);
}