#pragma once
#include <dr_graphics_defines.h>
#include <gtest\gtest.h>

TEST(Device, destructor) {
  EXPECT_TRUE(true);
}

TEST(Device, create) {
  EXPECT_TRUE(true);
}

TEST(Device, release) {
  EXPECT_TRUE(true);
}

TEST(Device, createVertexBuffer) {
  EXPECT_TRUE(true);
}

TEST(Device, createIndexBuffer) {
  EXPECT_TRUE(true);
}

TEST(Device, createConstantBuffer) {
  EXPECT_TRUE(true);
}

TEST(Device, createShaderFromMemory) {
  EXPECT_TRUE(true);
}

TEST(Device, createBlendState) {
  EXPECT_TRUE(true);
}

TEST(Device, createSamplerState) {
  EXPECT_TRUE(true);
}

TEST(Device, createRasteizerState) {
  EXPECT_TRUE(true);
}

TEST(Device, createTexture) {
  EXPECT_TRUE(true);
}

TEST(Device, createDepthStencilState) {
  EXPECT_TRUE(true);
}