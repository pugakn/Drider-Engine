#pragma once
#include <dr_graphics_defines.h>
#include <gtest\gtest.h>

TEST(DeviceContext, destructor) {
  EXPECT_TRUE(true);
}

TEST(DeviceContext, create) {
  EXPECT_TRUE(true);
}

TEST(DeviceContext, release) {
  EXPECT_TRUE(true);
}

TEST(DeviceContext, generateMipMaps) {
  EXPECT_TRUE(true);
}

TEST(DeviceContext, updateTextureFromMemory) {
  EXPECT_TRUE(true);
}

TEST(DeviceContext, updateBufferFromMemory) {
  EXPECT_TRUE(true);
}

TEST(DeviceContext, setRenderTargets) {
  EXPECT_TRUE(true);
}

TEST(DeviceContext, setTexture) {
  EXPECT_TRUE(true);
}

TEST(DeviceContext, setShader) {
  EXPECT_TRUE(true);
}

TEST(DeviceContext, setIndexBuffer) {
  EXPECT_TRUE(true);
}

TEST(DeviceContext, setVertexBuffer) {
  EXPECT_TRUE(true);
}

TEST(DeviceContext, setConstantBuffer) {
  EXPECT_TRUE(true);
}

TEST(DeviceContext, clearDepthStencilView) {
  EXPECT_TRUE(true);
}

TEST(DeviceContext, clearRenderTargetView) {
  EXPECT_TRUE(true);
}

TEST(DeviceContext, setPrimitiveTopology) {
  EXPECT_TRUE(true);
}

TEST(DeviceContext, draw) {
  EXPECT_TRUE(true);
}