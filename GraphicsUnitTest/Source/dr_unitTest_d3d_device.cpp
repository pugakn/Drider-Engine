#pragma once
#include <dr_device.h>
#include <gtest\gtest.h>

TEST(CommandBuffer, createVertexBuffer) {
  EXPECT_TRUE(true);
}

TEST(CommandBuffer, createIndexBuffer) {
  EXPECT_TRUE(true);
}

TEST(CommandBuffer, createConstantBuffer) {
  EXPECT_TRUE(true);
}

TEST(CommandBuffer, createShaderFromMemory) {
  EXPECT_TRUE(true);
}

TEST(CommandBuffer, createBlendState) {
  EXPECT_TRUE(true);
}

TEST(CommandBuffer, createSamplerState) {
  EXPECT_TRUE(true);
}

TEST(CommandBuffer, createRasteizerState) {
  EXPECT_TRUE(true);
}

TEST(CommandBuffer, createDepthStencilState) {
  EXPECT_TRUE(true);
}

TEST(CommandBuffer, createTexture) {
  EXPECT_TRUE(true);
}