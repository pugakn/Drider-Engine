#pragma once
#include <dr_texture.h>
#include <gtest\gtest.h>

TEST(RenderTarget, destructor) {
  EXPECT_TRUE(true);
}

TEST(RenderTarget, create) {
  EXPECT_TRUE(true);
}

TEST(RenderTarget, set) {
  EXPECT_TRUE(true);
}

TEST(RenderTarget, release) {
  EXPECT_TRUE(true);
}
