#pragma once
#include <dr_texture.h>
#include <gtest\gtest.h>

TEST(DepthStencil, destructor) {
  EXPECT_TRUE(true);
}

TEST(DepthStencil, create) {
  EXPECT_TRUE(true);
}

TEST(DepthStencil, release) {
  EXPECT_TRUE(true);
}