#pragma once
#include <dr_graphics_defines.h>
#include <gtest\gtest.h>

TEST(Buffer, destructor) {
  EXPECT_TRUE(true);
}

TEST(Buffer, create) {
  EXPECT_TRUE(true);
}

TEST(Buffer, release) {
  EXPECT_TRUE(true);
}