#pragma once
#include <dr_hull_shader.h>
#include <gtest\gtest.h>

TEST(D3DHullShader, set) {
  EXPECT_TRUE(true);
}

TEST(D3DHullShader, release) {
  EXPECT_TRUE(true);
}

TEST(D3DHullShader, createFromMemory) {
  EXPECT_TRUE(true);
}
