#pragma once
#include <dr_vertex_buffer.h>
#include <gtest\gtest.h>

TEST(D3DTextreShader, set) {
  EXPECT_TRUE(true);
}

TEST(D3DTextreShader, release) {
  EXPECT_TRUE(true);
}

TEST(D3DTextreShader, create) {
  EXPECT_TRUE(true);
}


